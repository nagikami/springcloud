server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/

feign:
  hystrix:
    enabled: true #开启feign维护的调用服务接口绑定fallback函数功能